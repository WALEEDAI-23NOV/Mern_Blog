<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body class="bg-zinc-900 text-white h-screen flex items-center justify-center">
    <div class="bg-zinc-800 p-8 rounded-xl shadow-lg w-full max-w-md">
        <h1 class="text-3xl font-semibold mb-6 text-center">Login</h1>
        <form action="/login" method="post" class="space-y-4">
            <input type="email" name="email" placeholder="Email" 
                   class="w-full px-4 py-2 bg-transparent border-2 rounded-lg border-zinc-600 focus:outline-none focus:border-blue-500">
            
            <input type="password" name="password" placeholder="Password" 
                   class="w-full px-4 py-2 bg-transparent border-2 rounded-lg border-zinc-600 focus:outline-none focus:border-blue-500">

            <button type="submit" 
                    class="w-full px-4 py-2 bg-blue-500 rounded-lg hover:bg-blue-600 transition">
                Login
            </button>
        </form>
        <p class="mt-4 text-center text-sm text-zinc-400">
            Don't have an account? 
            <a href="/" class="text-blue-400 hover:underline">Register</a>
        </p>
    </div>
</body>
</html>